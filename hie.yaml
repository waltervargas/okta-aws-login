cradle:
  cabal:
    - path: "src"
      component: "lib:okta-aws-login"

    - path: "app"
      component: "exe:okta-aws-login"

    - path: "test"
      component: "test:okta-aws-login-test"

dependencies:
  - package.yaml
  - shell.nix
  - default.nix
